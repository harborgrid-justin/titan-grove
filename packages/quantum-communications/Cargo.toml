[package]
name = "titan-grove-quantum-communications"
version = "1.0.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { version = "2.13.0", default-features = false, features = ["napi4"] }
napi-derive = "2.13.0"

[build-dependencies]
napi-build = "2.0.1"